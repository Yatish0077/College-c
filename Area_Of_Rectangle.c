#include <stdio.h>
int main(){
    int length,bredth;
    printf("Enter the length amd breath of rectangle : ");
    scanf("%d %d",&length,&bredth);
    printf("The Area of rectangle is %d",length*bredth);
    return 0;
}